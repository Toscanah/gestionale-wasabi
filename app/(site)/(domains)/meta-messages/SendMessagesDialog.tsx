import { Button } from "@/components/ui/button";
import DialogWrapper from "../../components/ui/dialog/DialogWrapper";

export default function SendMessagesDialog() {
  return <DialogWrapper trigger={<Button>Manda messaggi</Button>} />;
}
